name: agenda_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.5.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^5.3.0
  firebase_core: ^3.5.0
  path: ^1.9.0
  sqflite: ^2.3.3+1
  bcrypt: ^1.1.3  
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.13.1  

flutter:
  uses-material-design: true

  assets:
    - assets/logo.jpg
    - assets/agenda.png

flutter_launcher_icons:
  android: true
  image_path: "assets/agenda.png"
